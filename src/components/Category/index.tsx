import { FunctionComponent } from 'react';

interface ShopCategoryProps {
    children: React.ReactNode;
}

const ShopCategory: FunctionComponent<ShopCategoryProps> = () => {
    return <section></section>;
};

export default ShopCategory;
